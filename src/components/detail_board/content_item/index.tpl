<div
  class="content-item {{ isCurrent }}"
  data-content="{{ content }}"
  data-price="{{ default_price }}"
  data-name="{{ name }}"
  data-pic="{{ pic }}">
  <span class="item-content">{{ content }}</span>
  <span class="item-price">{{ price }}</span>
</div>